import Container from 'components/container';
import { EXAMPLE_PATH } from 'lib/constants';

const Footer = () => {
	return (
		<footer className='bg-blizzardBlue'>
			<Container>
				
			</Container>
		</footer>
	);
};

export default Footer;
